mixin breadcrumbs_from(bcList)
  - var sep = ""
  ul.breadcrumbs
    each bc, index in bcList
      unless index == 0
        li.spacer!= sep 
      li: a(href="#{bc.url}") #{bc.name}
      - sep = "&gt;"
